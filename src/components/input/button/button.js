import React from 'react';
import Button from '@material-ui/core/Button';

export default function Btn() {
    return (
        <Button variant="contained" color="primary" style={{height:'55px', marginTop:'9px'}} >
            Add
        </Button>
    );
}
